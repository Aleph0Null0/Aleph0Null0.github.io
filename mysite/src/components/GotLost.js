import { Link } from "react-router-dom";
import { Container } from "@mui/material";

export default function GotLost() {
    return (
        <>
        <Container sx={{ padding: "1rem", minHeight: "100vh" }}>
            <p>Looks like you're lost...</p>
        </Container>
            </>
    )
};
